var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { traverseAsync } from '../utils';
import { processLayer } from './processLayer';
export function addLayersToFrame(layers, baseFrame, onLayerProcess) {
    return __awaiter(this, void 0, void 0, function* () {
        for (const rootLayer of layers) {
            yield traverseAsync(rootLayer, (layer, parent) => __awaiter(this, void 0, void 0, function* () {
                try {
                    const node = yield processLayer(layer, parent, baseFrame);
                    onLayerProcess === null || onLayerProcess === void 0 ? void 0 : onLayerProcess({ node, layer, parent });
                }
                catch (err) {
                    console.warn('Error on layer:', layer, err);
                }
            }));
        }
    });
}
export * from './getFont';
export * from './dropOffset';
